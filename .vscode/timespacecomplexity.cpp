// time complexity  : It is the time required to rum an algorithim 
// space complecity  : 
// here the time complexity of the following program  is  O(n2) : because here 2 loops are used 
// {
    // Iterate through all the pairs
//     for (int i = 0; i < n; i++)
//         for (int j = 0; j < n; j++)
 
//             // Check if the sum of the pair
//             // (a[i], a[j]) is equal to z
//             if (i != j && a[i] + a[j] == z)
//                 return true;
 
//     return false;
// }
// here the time complexity of the two loops are 2*O(n) which is equals to O(n) as 2 i contant 
// count = 0
// for (int i = N; i > 0; i /= 2)
//   for (int j = 0; j < i; j++)
//     count++;
// Space complexity : space required by an above algorithim to run a special function for a given amount of input
// auxillary space : this is the  extra space needed to fill the while the  the function is running 
//  where as space comp calculates the whole space for a function 

 

